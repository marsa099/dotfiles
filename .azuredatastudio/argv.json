// This configuration file allows you to pass permanent command line arguments to VS Code.
// Only a subset of arguments is currently supported to reduce the likelihood of breaking
// the installation.
//
// PLEASE DO NOT CHANGE WITHOUT UNDERSTANDING THE IMPACT
//
// NOTE: Changing this file requires a restart of VS Code.
{
	// Use software rendering instead of hardware accelerated rendering.
	// This can help in cases where you see rendering issues in VS Code.
	// "disable-hardware-acceleration": true,

	// Disable sending crash reports to Microsoft telemetry.
	"enable-crash-reporter": false,

	// Force Electron to use libsecret instead of letting it auto-detect from
	// XDG_CURRENT_DESKTOP. On niri (and any desktop that isn't GNOME/Unity/KDE)
	// the auto-detection would otherwise fall back to "basic" plaintext storage
	// — silently, with no popup to warn that secrets are no longer encrypted.
	//
	// Note: this setting alone is not sufficient on NixOS — the nixpkgs
	// azuredatastudio derivation also needs libsecret added to its rpath
	// (handled in modules/azure-data-studio.nix), otherwise Chromium's OSCrypt
	// can't dlopen libsecret-1.so.0 and falls back anyway.
	"password-store": "gnome-libsecret"
}
