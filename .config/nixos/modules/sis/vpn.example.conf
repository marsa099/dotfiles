# Copy to vpn.local.conf and replace the placeholder host before rebuilding.
# Connection settings only: never add passwords or tokens to a Nix input.
host = vpn.example.invalid
set-routes = 0
pppd-ipparam = sis
