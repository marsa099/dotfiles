[
  # Copy to routes.local.nix and replace this documentation-only subnet.
  "192.0.2.0/24" # Example VPN route; use an actual work subnet before rebuilding.
] # Local values stay out of Git but still enter the local Nix store.
