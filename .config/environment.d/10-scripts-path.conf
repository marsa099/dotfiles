# Prepend ~/.scripts to PATH for the whole graphical session so wrapper
# scripts there (e.g. `config`) resolve in every shell — including
# non-interactive ones, where .bashrc returns early and never runs.
PATH=${HOME}/.scripts:${PATH}
